HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,https://www.tnt-mkr.com

# Database SSL — set to false only if your DB provider uses self-signed certs
# DATABASE_SSL_REJECT_UNAUTHORIZED=true

# Redis TLS — set to false only if your Redis provider uses self-signed certs
# REDIS_TLS_REJECT_UNAUTHORIZED=true

# Email
# DEFAULT_FROM_EMAIL=TNT MKR <no-reply@tnt-mkr.com>
# DEFAULT_REPLY_TO_EMAIL=customer-service@tnt-mkr.com
